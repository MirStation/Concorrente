#!/bin/sh

cont=0
factor=0
nofexp=5
for i in `seq 0 $1`
do
	factor=$(($i * $2))
	for j in `seq 1 $nofexp`
	do
		cont=$(($cont + `./parte2 $factor`))
	done
	cont=$(($cont / $nofexp))
	echo "$factor \t $cont"
done
